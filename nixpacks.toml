[providers]
python = "python"
node = "node"

[phases.setup]
nixPkgs = ["python311", "nodejs"]

[phases.build]
cmds = ["npm run build", "pip install -r requirements.txt"]

[start]
cmd = "python server.py"
